import { createTheme } from "@mui/material/styles";

export const TypographyTheme = createTheme({
    typography: {
        fontFamily: [
            'Nunito',
            'Arial',
            'Roboto'
        ].join(',')
    },

})